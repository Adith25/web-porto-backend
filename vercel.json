{
  "buildCommand": "npm run build",
  "functions": {
    "api/[...route].ts": {
      "maxDuration": 30
    }
  },
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "api/[...route].ts"
    }
  ]
}