{
  "buildCommand": "npm run build",
  "functions": {
    "api/[...route].ts": {
      "maxDuration": 30
    }
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/[...route]"
    }
  ]
}
